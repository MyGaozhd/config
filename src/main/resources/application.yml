spring:
  application:
    name: config
  profiles:
    active: native
server:
  port: 9006
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/
  instance:
    prefer-ip-address: true